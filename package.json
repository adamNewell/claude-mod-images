{
  "name": "claude-mod-images",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.4.0",
  "scripts": {
    "vendor": "bun scripts/vendor.ts",
    "typecheck": "tsc -p .",
    "validate": "claude plugin validate .claude-plugin/plugin.json && claude plugin validate .claude-plugin/marketplace.json",
    "test": "claude plugin test .",
    "check": "bun run typecheck && bun run validate && bun run test"
  },
  "devDependencies": {
    "fast-png": "8.0.0",
    "jpeg-js": "0.4.4",
    "omggif": "1.0.10",
    "typescript": "^5.9.0"
  }
}
